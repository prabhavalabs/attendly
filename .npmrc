# React Native / Expo autolinking expects a flat node_modules layout, so the
# monorepo uses pnpm's hoisted linker (Expo's documented setup for pnpm
# monorepos: https://docs.expo.dev/guides/monorepos/). The admin (Vite) and API
# (Workers) packages are unaffected by hoisting.
node-linker=hoisted
