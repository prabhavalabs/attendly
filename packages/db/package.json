{
  "name": "@tuition/db",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "migrate:local": "wrangler d1 migrations apply tuition-db --local",
    "migrate:remote": "wrangler d1 migrations apply tuition-db --remote"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
